```python
def parse_laby(laby):
    laby = self.labyrinthe
    for line in laby[:len(laby)]:
        if line == 'm':
        	laby[(x, y)] = "#"
        if line == 'c':
        	laby[(x, y)] = " "
        if line == 'a':
        	laby[(x, y)] = " ":

```